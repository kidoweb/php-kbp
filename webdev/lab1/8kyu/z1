def remove_every_other(arr):
    """
    Удаляет каждый второй элемент из массива, начиная со второго элемента.
    
    Аргументы:
    arr (list): Входной массив
    
    Возвращает:
    list: Массив с удаленными каждыми вторыми элементами
    """
    return arr[::2]

# Пример использования
example_array = ["Keep", "Remove", "Keep", "Remove", "Keep", "Remove", "Keep", "Remove"]
result = remove_every_other(example_array)

print("Исходный массив:")
print(example_array)
print("\nМассив после удаления:")
print(result)
